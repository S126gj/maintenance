spring:
  datasource:
    driver-class-name: org.apache.shardingsphere.driver.ShardingSphereDriver
    url: jdbc:shardingsphere:classpath:sharding/sharding-prod.yaml
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0
      #连接超时时间
      timeout: 8000ms
      jedis:
        pool:
          max-active: 50

file:
  # minio 文件上传
  minio:
    url: http://localhost:9000
    accessKey: minioadmin
    secretKey: minioadmin
    bucketName: maintenance
  # oss 文件上传
  oss:

############## Sa-Token 配置 (文档: https://sa-token.cc) ##############
sa-token:
  # token名称 (同时也是cookie名称)
  token-name: Authorization
  # token有效期，单位s 7天
  timeout: 10080
  # token临时有效期 (指定时间内无操作就视为token过期) 单位: 秒
  activity-timeout: -1
  # 是否允许同一账号并发登录 (为true时允许一起登录, 为false时新登录挤掉旧登录)
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个token (为true时所有登录共用一个token, 为false时每次登录新建一个token)
  is-share: true
  # token风格
  token-style: tik
  # 是否输出操作日志
  is-log: true

# xxljob  此项目暂时未启用xxljob，如若需要可自行开启
xxl:
  job:
    # 是否启用 Y 启用，N禁用
    enable: N
    accessToken: token
    admin:
      addresses: http://localhost:8080/xxl-job-admin
    executor:
      appname: maintenance-xxl-job-executor
      # 尽量不要指定address。不能注掉，否则报错
      address:
      ip: localhost
      port: 7010
      logpath: /Users/shenguoji/日志/xxl-job/jobhandler
      logretentiondays: 30

# 短信
sms:
  # 腾讯短信服务
  tx:
    secretId:
    secretKey:
    endpoint:
    region:
    sdkAppId:
    signName:
    # 登录验证码短信模板
    loginVerifyTemplateId:
  # 容联云短信服务
  rly:
    serverIp:
    serverPort:
    accountSId:
    accountToken:
    appId:
    # 登录验证码短信模板
    loginVerifyTemplateId:

# springdoc-openapi
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan:
        - com.device.mbg.controller
        - com.device.file.controller
        - com.device.system.column.controller
        - com.device.system.core.controller
        - com.device.system.staff.controller
        - com.device.system.customer.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

# forest 默认域名配置
forest:
  variables:
    # 可指定多个client
    apiUrl: http://localhost

# elasticsearch
# es-head连接路径: http://123.60.49.164:9200/?auth_user=elastic&auth_password=Q2NEUIKqTaaLUXCCe3qz
# kibana 地址 http://123.60.49.164:5601
easy-es:
  enable: true
  address: localhost:9200
  username: elastic
  password: